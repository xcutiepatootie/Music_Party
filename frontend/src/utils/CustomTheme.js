import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary_g: {
      main: "#ACE1AF", // Custom primary color
    },
    secondary_g: {
      main: "#B0EBB4", // Custom secondary color
    },
    tertiary_g: {
      main: "#BFF6C3", // Custom secondary color
    },
    quad_g: {
      main: "#E0FBE2", // Custom secondary color
    },
  },
});
